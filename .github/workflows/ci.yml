name: CI master
run-name: CI master tests
on:
  workflow_dispatch:
  push:
    branches:
      - master
    paths:
      - '**.zig'
      - '**.zon'
  schedule:
    - cron: '0 0 * * 0' # Runs every Sunday at 00:00 UTC
jobs:
  test:
    runs-on: ubuntu-latest
    name: Test master on linux
    timeout-minutes: 2
    strategy:
      matrix:
        zig_version: ["master"]
    steps:
      - name: "Checkout repo"
        uses: actions/checkout@v4
      - name: "Setup zig environment"
        uses: mlugg/setup-zig@v2
        with:
          version: ${{ matrix.zig_version }}
      - name: "Run linter"
        run: zig build lint
